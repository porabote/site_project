import * as React from 'react';

type ContactsType = {
  title: string;
};

const Contacts = (props: ContactsType) => {
  return (
    <div>
      contacts
    </div>
  );
};

export default Contacts;